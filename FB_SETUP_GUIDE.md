# 🔵 ফেসবুক পেজ অটো-পোস্টিং সেটআপ গাইড (BNE)

এই গাইড অনুসরণ করলে আপনার ফেসবুক পেজে টেলিগ্রাম চ্যানেলের মতোই **প্রিভিউসহ স্বয়ংক্রিয় নিউজ পোস্ট** চালু হবে।
প্রয়োজনীয় সময়: ১৫–২০ মিনিট। খরচ: ০ টাকা।

> ⚠️ **কেন টোকেন লাগবে:** আপনার পেজের পক্ষে পোস্ট করার অনুমতি একটি **Page Access Token** দিয়ে দেওয়া হয়।
> টোকেন GitHub Actions-এর Secret হিসেবে থাকবে — কোডে বা সাইটে কখনো প্রকাশ হবে না।

---

## ধাপ ১ — ফেসবুক ডেভেলপার অ্যাপ তৈরি

1. [developers.facebook.com](https://developers.facebook.com) খুলুন → উপরে **My Apps** → **Create App**
2. **Use case** জিজ্ঞেস করলে **"Other"** নির্বাচন করুন → Next
3. App name দিন (যেমন: `BNE Auto Poster`), যোগাযোগ ইমেইল দিন → **Create App**
4. বাম মেনুতে **App Settings → Basic**-এ গিয়ে আপনার **App ID** ও **App Secret** নোট করে রাখুন (পরে লাগবে)

## ধাপ ২ — Graph API Explorer দিয়ে পেজ টোকেন নেওয়া

1. [developers.facebook.com/tools/explorer](https://developers.facebook.com/tools/explorer) খুলুন
2. উপরে ডানদিকে আপনার নতুন অ্যাপটি সিলেক্ট করুন
3. **Permissions** বাটনে ক্লিক করে এই ২টি পারমিশন যোগ করুন:
   - `pages_manage_posts` (পেজে পোস্ট করার অনুমতি)
   - `pages_read_engagement` (পেজ রিড অ্যাক্সেস)
4. **Generate Access Token** চাপুন → ফেসবুক লগইন পপআপে **পেজ অ্যাডমিন অ্যাকাউন্ট** দিয়ে অনুমোদন দিন
5. এবার উপরের **Token ড্রপডাউনে** আপনার **পেজটি** সিলেক্ট করুন (ব্যবহারকারী নয়, পেজ!) → টোকেনটি পেজ টোকেন হয়ে যাবে
6. টোকেনটি কপি করে রাখুন (এটি **short-lived**, ~১-২ ঘণ্টা চলবে)

## ধাপ ৩ — টোকেন ৬০ দিনের (long-lived) বানানো

শর্ট-লাইভড টোকেন প্রতি ৬০ দিনে রিফ্রেশ করতে হবে — এজন্য long-lived টোকেন বানিয়ে নিন:

ব্রাউজারে এই URL-টি খুলুন (নিজের মান বসিয়ে):

```
https://graph.facebook.com/v26.0/oauth/access_token?grant_type=fb_exchange_token&client_id=YOUR_APP_ID&client_secret=YOUR_APP_SECRET&fb_exchange_token=YOUR_PAGE_TOKEN
```

ফলাফলে `access_token`-এর মানটিই আপনার **long-lived পেজ টোকেন** (~৬০ দিনের)। এটি কপি করে রাখুন।

> 📌 **পেজ আইডি:** আপনার পেজের URL থেকে নিতে পারেন — যেমন `facebook.com/MyPageName` → পেজ আইডি `MyPageName` অথবা পেজ About-এ থাকা সংখ্যাটি। Graph Explorer-এ `GET /me` দিলেও পেজ আইডি দেখাবে।

## ধাপ ৪ — GitHub Secrets-এ যোগ করুন

প্রজেক্ট রিপো: `oumaboy93-alt/bangla-news-edition`

**বিকল্প A (নিজে করুন):**
1. GitHub → রিপো → **Settings → Secrets and variables → Actions → New repository secret**
2. এই নামগুলো যোগ করুন:

| Secret নাম | মান |
|-----------|-----|
| `FB_PAGE_TOKEN` | ধাপ ৩-এর long-lived টোকেন |
| `FB_PAGE_ID` | আপনার পেজ আইডি |
| `TELEGRAM_BOT_TOKEN` | [@BotFather](https://t.me/BotFather) থেকে নতুন বট টোকেন (পুরনো লিক হওয়া টোকেন **বাতিল করে নতুন নিন** — নিরাপত্তার জন্য) |
| `TELEGRAM_CHAT_ID` | `@bne0999` (বা আপনার চ্যানেলের হ্যান্ডেল) |
| `FB_POST_MODE` *(ঐচ্ছিক)* | `link` = OG প্রিভিউ কার্ড (ডিফল্ট, প্রস্তাবিত) · `photo` = বড় ছবি+ক্যাপশন |
| `MAX_POSTS_PER_RUN` *(ঐচ্ছিক)* | প্রতি রানে সর্বোচ্চ পোস্ট সংখ্যা (ডিফল্ট `5`) |
| `IG_USER_ID` *(ঐচ্ছিক)* | ইনস্টাগ্রাম প্রফেশনাল অ্যাকাউন্টের user id — সেট করলে একই FB টোকেনে IG-তেও পোস্ট |
| `TELEGRAM_ADMIN_CHAT_ID` *(ঐচ্ছিক)* | টোকেন-এক্সপায়ারি/এরর হলে আপনার টেলিগ্রামে অ্যালার্ট আসবে |
| `EMAIL_OCTOPUS_API_KEY` / `EMAIL_OCTOPUS_LIST_ID` *(ঐচ্ছিক)* | নিউজলেটার ফর্ম আসল সাবস্ক্রাইব করতে (ফ্রি ২৫০০ সাবস্ক্রাইবার) |

**বিকল্প B (আমাকে দিন):** টোকেন ও পেজ আইডি আমাকে জানালে আমি `gh` CLI দিয়ে সরাসরি Secrets সেট করে দেব — আপনি নিজেও পরে যেকোনো সময় বদলাতে পারবেন।

## ধাপ ৫ — প্রথমবার প্রিভিউ নিশ্চিত করা

- ফেসবুক প্রথমবার লিংকের প্রিভিউ স্ক্র্যাপ করে — প্রথম পোস্টে প্রিভিউ না দেখালে [Facebook Sharing Debugger](https://developers.facebook.com/tools/debug/) খুলে আপনার আর্টিকেল URL পেস্ট করে **Scrape Again** চাপুন
- আমাদের সাইটে এখন **ডায়নামিক OG ট্যাগ** আছে (প্রতিটি আর্টিকেলে নিজস্ব ছবি+শিরোনাম+সারাংশ) — তাই পরবর্তী পোস্টগুলোতে স্বয়ংক্রিয় প্রিভিউ আসবে

## কীভাবে কাজ করে (আর্কিটেকচার)

```
GitHub Actions (প্রতি ৩০ মিনিটে)
  → auto_social_poster.js (V6)
      → ৯টি RSS ফিড থেকে নতুন সংবাদ (last_posted.json দিয়ে ডিডুপ)
      → প্রতিটি খবর:
          টেলিগ্রাম → sendPhoto (ছবি+ক্যাপশন) → ব্যর্থ হলে টেক্সট
          ফেসবুক   → /v26.0/{page-id}/feed (message + link → OG প্রিভিউ কার্ড)
                     বা FB_POST_MODE=photo → /photos (ছবি+ক্যাপশন)
  → last_posted.json আপডেট (কোনো খবর দ্বিতীয়বার পোস্ট হয় না)
```

## টোকেন মেয়াদ শেষ হলে

Long-lived টোকেন **৬০ দিন** পর মেয়াদ শেষ হয়। তখন ধাপ ২-৩ পুনরাবৃত্তি করে নতুন টোকেন Secret-এ আপডেট করুন।
(প্রতিটি রানে Actions-এর লগে `[Facebook] ... ব্যর্থ` দেখালেই বুঝবেন টোকেন রিফ্রেশ করতে হবে।)
