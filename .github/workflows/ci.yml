name: CI — Quality Gate

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: JS Syntax Check (সব .js)
        run: |
          set -e
          for f in $(find . -name '*.js' -not -path './node_modules/*' -not -path './.git/*'); do
            node --check "$f" || { echo "সিনট্যাক্স ব্যর্থ: $f"; exit 1; }
          done
          echo "✅ সব JS সিনট্যাক্স ঠিক"

      - name: JSON Validation
        run: |
          node -e "
            const fs = require('fs');
            // admin-manifest.json / .webmanifest সরানো হয়েছে (অবসরপ্রাপ্ত
            // অ্যাডমিন প্যানেল)। তালিকা থেকে বাদ, নইলে CI অকারণে ব্যর্থ হত।
            const files = ['data/bne-config.json','manifest.webmanifest','last_posted.json','sponsor_leads_outreach.json'];
            for (const f of files) { if (fs.existsSync(f)) JSON.parse(fs.readFileSync(f, 'utf8')); }
            console.log('✅ সব JSON বৈধ');
          "

      - name: Unit Tests (Core Logic)
        run: node tests/core.test.js

      # ══════════════════════════════════════════════════════════════════
      # P0 রিগ্রেশন-প্রাচীর — যে চারটি বাগ একবার লাইভ সাইট ভেঙেছিল, সেগুলো
      # আর কখনো নিঃশব্দে ফিরে আসতে পারবে না। প্রতিটি পরীক্ষা একটি প্রকৃত
      # ঘটনার বিপরীতে লেখা।
      # ══════════════════════════════════════════════════════════════════
      - name: P0 Regression Wall
        run: node tests/p0-regression.js

      - name: Secret Scan (gitleaks)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
